<?php
  /**
   * implementing hook_theme().
   * */

  function ellinight_theme($existing, $type, $theme, $path) {
    return array(
      'article_list' => array(
        'variables' => array('items' => array(), 'title' => '')
      )
    );
  }
